# WorkBreak — dipendenze Python
# Installa con: pip install -r requirements.txt

# UI e tray
pillow>=10.0.0
pystray>=0.19.0

# Rilevamento webcam (opzionale ma consigliato)
opencv-python>=4.8.0
mediapipe>=0.10.0

# Notifiche (fallback se notify-send non disponibile)
plyer>=2.1.0
