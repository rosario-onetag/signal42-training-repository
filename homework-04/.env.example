# Copy to `.env` for local development. In Fargate these values come from the
# task definition (plain env) and AWS Secrets Manager for anything sensitive.

# HTTP port the container listens on (TLS is terminated at the load balancer).
PORT=8802

# MongoDB
DATABASE_URL=mongodb://127.0.0.1:27017/
DATABASE_NAME=sellers-data
